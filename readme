📝 Полнофункциональное приложение списка дел с системой аутентификации на основе Supabase.

🚀 Технологии
Frontend: React 18, Vite
Backend: Supabase (PostgreSQL + Auth)
Аутентификация: Supabase Auth с подтверждением email
Стилизация: CSS Modules, современный адаптивный дизайн
Хостинг: Vercel (готово к деплою)

✨ Возможности
✅ Регистрация с подтверждением email
✅ Вход по email и паролю
✅ Защищенные маршруты через AuthGate
✅ Управление профилем пользователя
✅ Современный UI/UX дизайн
✅ Полностью адаптивный интерфейс
✅ Политика конфиденциальности в модальном окне
✅ Валидация форм

📁 Структура проекта
text
ToDotist_Auth/
├── src/
│   ├── components/
│   │   ├── AuthGate.jsx          # Компонент защиты маршрутов
│   │   ├── Login.jsx            # Компонент входа/регистрации
│   │   └── Profile.jsx          # Компонент профиля пользователя
│   ├── App.jsx                  # Главный компонент приложения
│   ├── index.jsx                # Точка входа
│   ├── index.css               # Глобальные стили
│   ├── supabase.js             # Конфигурация Supabase клиента
│   └── Login.css              # Стили компонента Login
├── public/
│   └── index.html              # HTML шаблон
├── .env.local                  # Переменные окружения (Supabase)
├── .gitignore                  # Git игнор файл
├── package.json                # Зависимости и скрипты
├── package-lock.json           # Лок файл зависимостей
├── vite.config.js              # Конфигурация Vite
└── README.md                   # Документация (этот файл)



🛠️ Установка и запуск
Предварительные требования:
 - Node.js (версия 16 или выше)
 - Аккаунт Supabase
 - Git

Шаг 1: Клонирование репозитория
bash
# Клонировать репозиторий
git clone <ваш-репозиторий>
cd ToDotist_Auth
Шаг 2: Установка зависимостей
bash
# Установить зависимости
npm install
Шаг 3: Настройка Supabase
Создайте проект на Supabase

Получите URL и Anon ключ из настроек проекта

Создайте таблицу Profiles:
sql
CREATE TABLE public.Profiles (
    id UUID REFERENCES auth.users(id) PRIMARY KEY,
    user_id UUID NOT NULL,
    email TEXT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

Включите подтверждение email в настройках Authentication

Шаг 4: Настройка переменных окружения
Создайте файл .env.local в корне проекта:

env
VITE_SUPABASE_URL=ваш_supabase_url
VITE_SUPABASE_ANON_KEY=ваш_anon_key
Шаг 5: Запуск проекта
bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Превью собранного проекта
npm run preview
Приложение будет доступно по адресу: http://localhost:5173

🔐 Аутентификация
Регистрация пользователя
Пользователь вводит email и пароль

Система отправляет письмо с подтверждением

Пользователь подтверждает email по ссылке из письма

Создается запись в таблице Profiles

Вход в систему
Валидация email и пароля

Автоматический редирект на защищенную страницу профиля

Защита маршрутов
Компонент AuthGate.jsx проверяет авторизацию пользователя и защищает доступ к приватным страницам.

📄 Политика конфиденциальности
Приложение включает полную политику конфиденциальности в соответствии с требованиями ФЗ-152 "О персональных данных". Политика доступна в модальном окне при регистрации.

Основные положения:
Сбор только email адреса для аутентификации

Хранение данных на защищенных серверах Supabase

Возможность отзыва согласия на обработку данных

Контакт для вопросов: andrunik2010@mail.ru

🌐 Развертывание на Vercel
Проект готов к развертыванию на Vercel:

Подключите ваш GitHub репозиторий к Vercel

Установите переменные окружения в настройках проекта:

VITE_SUPABASE_URL

VITE_SUPABASE_ANON_KEY

Нажмите Deploy
